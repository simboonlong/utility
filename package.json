{
  "name": "@simboonlong/utility",
  "version": "0.2.3",
  "description": "Some web utility functions to use.",
  "main": "index.js",
  "scripts": {
    "serve": "npx http-server",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "e2e:local": "concurrently \"npm:serve\" \"npm:cy:open\"",
    "e2e:ci": "concurrently -k \"npm:serve\" \"npm:cy:run\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sim Boon Long",
  "license": "MIT",
  "homepage": "https://github.com/simboonlong/utility",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  },
  "devDependencies": {
    "@testing-library/cypress": "^7.0.1",
    "concurrently": "^5.3.0",
    "cypress": "^5.2.0"
  }
}
